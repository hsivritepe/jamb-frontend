// Основной entry-пойнт (CLI-скрипт) для запуска импорта

// 	•	Точка входа. Будет запускаться командой ts-node server/finishing-material-import/index.ts (или через скрипт в package.json).
//	•	Внутри вызывает функции из importer.ts, обрабатывает возможные ошибки.