// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["dev.thejamb.com"],
    },
    reactStrictMode: true,
  };
  
  export default nextConfig;